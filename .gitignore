target/
.idea/
.classpath
.project
.settings/
out/
tags
world/
tmp/
*.iml

cache/

*.dat

*.mca

dependency-reduced-pom.xml

# Docker web console runtime data (worlds, registry cache, auth session, settings)
# Anchored to the repo root so it doesn't also match the src/main/java/game/data package.
/data/

# .NET / WinUI desktop build output
desktop/bin/
desktop/obj/
publish/
*.user

.tmp_build/

# scrubbed: local analysis artifacts (do not commit)
saved_items_report.txt
lock_report.txt
scan_saved_items.py
scan_locks.py
scan_area.py
check_locksigns.py
